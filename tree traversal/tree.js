// linear maintains order but non linear does not maintain the order

// siblings = children of same parents.
// hight of the tree = maximum 

// A tree is called binary tree if each node pointing maximum two nodes(zero, one, two)